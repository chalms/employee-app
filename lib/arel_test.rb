class ArelTest
  def self.go

    puts users2.count

    # query = users_reports.where().where(users[:type].eq('Employee')).project(users[:id])
    # users = User.find_by_sql(query.to_sql)
    # puts users.inspect
  end
end