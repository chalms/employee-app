#user-data{user_hash: @user}
	-# :javascript
	-# 	Metrics.setUser = function() { Metrics.Data.user.set(#{@user ? @user.to_json(root: false) : {} } ) }
	-# 	$(document).ready(function(){
	-# 	  if (Backbone.History.started == false) {
	-# 	    Metrics.initialize();
	-# 	  }
	-# 	});
#session-token{token: @token.to_json}