%table.table.table-hover
  %tr.headers
    %th Name
    %th Assigned Reports
    %th Completed Reports
    %th Task Completion %
    %th Hours
    %th Days Working on #{name}
  - employees.each do |employee|
    %tr.table-row
      %td= link_to employee.name, "employees/#{employee.id}", remote: true
      = render partial: 'employees/table_row', locals: { employee: employee, options: options, type: type}